query ViewerUserInfo {
    Viewer {
        id
        name
        avatar {
            large
        }
        bannerImage
        about(asHtml: true)
        options {
            profileColor
            titleLanguage
        }
        mediaListOptions {
            scoreFormat
        }
        siteUrl
    }
}
